# C6 Execution Contract

Implement exact financial values, deterministic CPU reference calculations, calibrated distributions, optimization feasibility, causal diagnostics and transaction-cost attribution.

Tests must cover exact round-trips, stable reductions, singular covariance, tail risk, deterministic simulation, calibration/abstention, infeasible constraints, causal assumptions and accelerator decision divergence.

Candidate libraries and devices remain unselected. Divergence causes quarantine and CPU fallback. Rollback restores previous registries while retaining differential evidence.

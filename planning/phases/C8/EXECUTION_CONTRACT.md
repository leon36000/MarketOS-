# C8 — Claude Code Execution Contract

## Objective

Implement point-in-time multi-scale features, explicit observation-detail adapters, uncertainty-aware execution research and consistent derivative surfaces.

## Required surfaces

```text
runtime/features/
runtime/microstructure/
runtime/execution_models/
runtime/derivatives/
tests/features/
tests/microstructure/
tests/derivatives/
benchmarks/C8/
```

## Test-first sequence

1. Future observations must be invisible to an earlier feature context.
2. Cross-frequency joins require explicit availability rules.
3. Lower-detail fixtures cannot close higher-detail behavior gates.
4. Sequence gaps, duplicates, late events and restarts must preserve deterministic state.
5. Simulated and observed outcomes cannot share a promotable classification.
6. Surface inputs must align identity, terms, times and adjustments.
7. Inconsistent surfaces must fail checks rather than be silently smoothed.
8. Model and solver uncertainty must propagate to reported sensitivities.

## Qualification boundary

No feed depth, execution model, derivative model or predictive value is selected by local fixtures.

## Rollback

Remove the signed C8 delta, restore the C7 checkpoint, retain all failed fits and divergence evidence, and regenerate derived files.

# Neon memory schema

Apply `001_memory_schema.sql` only through a reviewed migration. Store the Neon connection URL in a secret manager or injected environment variable; never commit it.

The current project is `frosty-boat-02108163`, database `neondb`, schema `marketos_memory`. Exact record counts are observational and must be refreshed rather than hard-coded as truth.

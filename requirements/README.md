# Requirements

`REQUIREMENT_CROSSWALK.csv` is the human-readable reconciled set of 108 owner/conversation requirements. `REQUIREMENTS_INDEX.json` is the compact machine index used during validation and context assembly. Direct memory-control requirements stored in Neon are tracked separately, so the Neon count is currently 111.
